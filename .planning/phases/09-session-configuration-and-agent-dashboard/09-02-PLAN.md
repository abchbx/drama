---
phase: 09-session-configuration-and-agent-dashboard
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/store/appStore.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/types.ts
  - frontend/src/components/config/LLMConfigTab.tsx
  - frontend/src/components/config/SessionParamsTab.tsx
autonomous: true
requirements: [UI-02, UI-03]
must_haves:
  truths:
    - User can select LLM provider (OpenAI, Anthropic, Mock)
    - User can view current LLM configuration
    - User can configure session parameters (duration, agent count)
    - Session params form has simple and advanced sections
    - Form validation ensures valid input
  artifacts:
    - frontend/src/store/appStore.ts: Updated with config state
    - frontend/src/lib/api.ts: Updated with config endpoints
    - frontend/src/lib/types.ts: Added config types
    - frontend/src/components/config/LLMConfigTab.tsx: LLM provider selection
    - frontend/src/components/config/SessionParamsTab.tsx: Session params form
  key_links:
    - from: LLMConfigTab.tsx
      to: appStore.ts
      via: zustand state management
      pattern: useAppStore
    - from: SessionParamsTab.tsx
      to: api.ts
      via: API client
      pattern: apiClient
---

<objective>
Implement LLM configuration and session parameters form with validation.

Purpose: Allow users to configure LLM provider and session settings
Output: LLM config UI, session params form, state management, API client extensions
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-session-configuration-and-agent-dashboard/09-CONTEXT.md
@.planning/phases/09-session-configuration-and-agent-dashboard/09-RESEARCH.md
@.planning/phases/09-session-configuration-and-agent-dashboard/09-01-PLAN.md

# Research Recommendations
- Use ReactHookForm with Zod validation
- Extend existing Zustand store for config state
- Keep form state local, store completed config in Zustand
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update types and API client</name>
  <files>frontend/src/lib/types.ts, frontend/src/lib/api.ts</files>
  <action>
    1. Add config-related types to types.ts:
       - LLMProvider type (openai/anthropic/mock)
       - LLMConfig interface
       - SessionParams interface
    2. Extend ApiClient in api.ts with config endpoints:
       - getConfig() - fetch current configuration
       - updateConfig() - update LLM and session params
  </action>
  <verify>
    <automated>npm run build