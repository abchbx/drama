---
phase: 10-real-time-visualization
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/src/components/visualization/CommunicationGraph.tsx"
]
autonomous: true
gap_closure: true
requirements: [UI-05]

must_haves:
  truths:
    - "User can visualize agent communication patterns as real-time graph"
  artifacts:
    - path: "frontend/src/components/visualization/CommunicationGraph.tsx"
      provides: "Agent communication graph with React Flow"
      min_lines: 150
  key_links:
    - from: "frontend/src/components/visualization/CommunicationGraph.tsx"
      to: "frontend/src/lib/socket.ts"
      via: "Socket.IO message:received event listener"
---

<objective>
Implement CommunicationGraph component with React Flow for visualizing agent message flow
Purpose: Replace placeholder with functional React Flow graph that displays agent communication patterns in real-time
Output: CommunicationGraph.tsx with node/edge management and Socket.IO integration
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/Coding/ClaudeCode/drama/frontend/src/components/dashboard/AgentGraph.tsx
@d:/Coding/ClaudeCode/drama/frontend/src/components/visualization/MessageStream.tsx
@d:/Coding/ClaudeCode/drama/frontend/src/components/visualization/CommunicationGraph.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CommunicationGraph with React Flow</name>
  <files>frontend/src/components/visualization/CommunicationGraph.tsx</files>
  <action>
    Replace the placeholder CommunicationGraph component with a full React Flow implementation:

    1. Import React Flow components (ReactFlowProvider, useNodesState, useEdgesState, Controls, Background, Handle, Position, MarkerType)
    2. Create a MessageNode component that displays sender/recipient info with message preview
    3. Use useNodesState and useEdgesState to manage graph state
    4. Listen to 'message:received' Socket.IO events to dynamically add nodes and edges
    5. When a message is received, ensure both sender and recipient nodes exist, create edges between them
    6. Implement pause functionality via isPaused prop - stop updating when paused
    7. Use ReactFlowProvider wrapper for proper React Flow context

    Key patterns from AgentGraph.tsx:
    - Use reactFlowWrapper ref for container
    - Use fitView on initial load
    - Handle cleanup in useEffect return

    Socket.IO integration pattern from MessageStream.tsx:
    - socketService.on('message:received', handler)
    - return () => socketService.off('message:received', handler)

    Node structure: Each agent gets a node. Edges represent message flow.
    - Nodes: id=agentId, data={name, role, messageCount}
    - Edges: source=sender, target=recipient, animated for recent messages
  </action>
  <verify>
    <automated>
      grep -n "ReactFlow" frontend/src/components/visualization/CommunicationGraph.tsx;
      grep -n "message:received" frontend/src/components/visualization/CommunicationGraph.tsx;
      grep -n "useNodesState" frontend/src/components/visualization/CommunicationGraph.tsx
    </automated>
  </verify>
  <done>CommunicationGraph displays agent communication as interactive React Flow graph with real-time updates</done>
</task>

</tasks>

<verification>
1. Verify React Flow imports are present
2. Verify Socket.IO listener for message:received is wired
3. Verify nodes and edges state management is implemented
4. Verify pause functionality stops updates
5. Verify component builds without errors
</verification>

<success_criteria>
- [ ] CommunicationGraph uses ReactFlowProvider and renders ReactFlow component
- [ ] Nodes are created dynamically from message senders/recipients
- [ ] Edges are created between communicating agents
- [ ] Socket.IO message:received events update the graph in real-time
- [ ] Pause functionality prevents updates when isPaused is true
- [ ] Component has proper Handle components for edge connections
</success_criteria>

<output>
After completion, create `.planning/phases/10-real-time-visualization/10-05-SUMMARY.md`
</output>
