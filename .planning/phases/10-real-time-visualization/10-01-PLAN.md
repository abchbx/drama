---
phase: 10-real-time-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/services/router.ts",
  "src/routes/blackboard.ts"
]
autonomous: true
requirements: [RT-03]

must_haves:
  truths:
    - "Frontend receives real-time message updates via Socket.IO"
    - "Frontend receives real-time memory state updates via Socket.IO"
  artifacts:
    - path: "src/services/router.ts"
      provides: "Socket.IO message broadcast functionality"
      contains: "message:received"
    - path: "src/routes/blackboard.ts"
      provides: "Socket.IO memory update broadcast functionality"
      contains: "memory:updated"
  key_links:
    - from: "src/services/router.ts"
      to: "Socket.IO"
      via: "_io.emit('message:received')"
    - from: "src/routes/blackboard.ts"
      to: "Socket.IO"
      via: "io.emit('memory:updated')"
---

<objective>
Add Socket.IO broadcast events for real-time message and memory state updates

Purpose: Enable the frontend to receive real-time updates about agent communication and memory state without polling API endpoints. This builds the foundation for the real-time visualization features.

Output: Updated backend services that emit Socket.IO events for message reception and memory changes
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/Coding/ClaudeCode/drama/.planning/STATE.md
@d:/Coding/ClaudeCode/drama/.planning/ROADMAP.md
@d:/Coding/ClaudeCode/drama/src/services/router.ts
@d:/Coding/ClaudeCode/drama/src/routes/blackboard.ts
</context>

<tasks>

<task type="auto">
  <name>Add message:received Socket.IO broadcast in RouterService</name>
  <files>src/services/router.ts</files>
  <action>
    In RouterService, modify the `routeMessage` method (and all send methods: sendPeerToPeer, sendMulticast, sendBroadcast) to emit a Socket.IO event 'message:received' with the full message object to all connected clients.

    Look at how agent_connected events are emitted (line 172-178) as a pattern:
    ```typescript
    this._io.emit('agent_connected', {
      agentId,
      role,
      socketId: socket.id,
      timestamp: Date.now(),
    });
    ```

    For message broadcasts, emit the full RoutingMessage object:
    ```typescript
    this._io.emit('message:received', routed);
    ```

    Add this after the message is routed in all send methods and routeMessage.
  </action>
  <verify>
    <automated>grep -n "message:received" src/services/router.ts</automated>
  </verify>
  <done>RouterService emits 'message:received' Socket.IO event for every routed message</done>
</task>

<task type="auto">
  <name>Add memory:updated Socket.IO broadcast in blackboard routes</name>
  <files>src/routes/blackboard.ts</files>
  <action>
    In blackboard.ts routes, add Socket.IO broadcast for memory state changes. The RouterService's io instance is available in the routes via app.locals or by importing the service.

    After any write operation (writeEntry, foldEntries, promoteEntry), emit:
    ```typescript
    io.emit('memory:updated', {
      dramaId,
      layer,
      timestamp: Date.now()
    });
    ```

    If you need access to the Socket.IO server, look at how it's done in src/index.ts where routerService is created and attached to the app.
  </action>
  <verify>
    <automated>grep -n "memory:updated" src/routes/blackboard.ts</automated>
  </verify>
  <done>Blackboard routes emit 'memory:updated' Socket.IO event on state changes</done>
</task>

</tasks>

<verification>
1. Run backend and verify Socket.IO events are emitted
2. Connect frontend Socket.IO client and listen for events
3. Trigger message routing and memory writes, verify events fire
</verification>

<success_criteria>
- [ ] 'message:received' events broadcast for every agent message
- [ ] 'memory:updated' events broadcast on memory state changes
</success_criteria>

<output>
After completion, create `.planning/phases/10-real-time-visualization/10-01-SUMMARY.md`
</output>
