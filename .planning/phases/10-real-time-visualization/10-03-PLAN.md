---
phase: 10-real-time-visualization
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified: [
  "frontend/src/components/visualization/CommunicationGraph.tsx"
]
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "User can view communication graph showing agent message flow"
  artifacts:
    - path: "frontend/src/components/visualization/CommunicationGraph.tsx"
      provides: "Agent communication graph visualization"
      min_lines: 80
      contains: "ReactFlow"
  key_links:
    - from: "frontend/src/components/visualization/CommunicationGraph.tsx"
      to: "frontend/src/components/dashboard/AgentGraph.tsx"
      via: "React Flow component reuse"
---

<objective>
Implement communication graph visualization
Purpose: Create interactive communication graph component that visualizes agent interactions and message flow using React Flow.
Output: CommunicationGraph component with full message flow implementation
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/Coding/ClaudeCode/drama/.planning/STATE.md
@d:/Coding/ClaudeCode/drama/.planning/ROADMAP.md
@d:/Coding/ClaudeCode/drama/frontend/src/components/dashboard/AgentGraph.tsx
@d:/Coding/ClaudeCode/drama/src/types/blackboard.ts
</context>

<tasks>

<task type="auto">
  <name>Create CommunicationGraph component with full implementation</name>
  <files>frontend/src/components/visualization/CommunicationGraph.tsx</files>
  <action>
    Create a communication graph component following the React Flow patterns from AgentGraph.tsx with full message flow implementation:
    ```typescript
    import { useState, useEffect } from 'react';
    import ReactFlow, {
      MiniMap,
      Controls,
      Background,
      useNodesState,
      useEdgesState,
      addEdge,
      Connection,
    } from 'reactflow';
    import 'reactflow/dist/style.css';
    import { socketService } from '../../lib/socket';
    import type { RoutingMessage } from '../../lib/types';
    import './visualization.css';

    interface CommunicationGraphProps {
      isPaused: boolean;
    }

    export function CommunicationGraph({ isPaused }: CommunicationGraphProps) {
      const [nodes, setNodes, onNodesChange] = useNodesState([]);
      const [edges, setEdges, onEdgesChange] = useEdgesState([]);

      useEffect(() => {
        const handleMessageReceived = (data: unknown) => {
          if (isPaused) return;

          const message = data as RoutingMessage;

          // Update graph with message flow
          // Add sender and receiver as nodes if they don't exist
          setNodes(prevNodes => {
            const newNodes = [...prevNodes];
            if (!newNodes.find(node => node.id === message.from)) {
              newNodes.push({
                id: message.from,
                type: 'input',
                position: { x: 250, y: newNodes.length * 100 },
                data: { label: message.from },
              });
            }
            if (!newNodes.find(node => node.id === message.to)) {
              newNodes.push({
                id: message.to,
                type: 'output',
                position: { x: 750, y: newNodes.length * 100 },
                data: { label: message.to },
              });
            }
            return newNodes;
          });

          // Add edge for communication
          setEdges(prevEdges => {
            const existingEdge = prevEdges.find(
              edge => edge.source === message.from && edge.target === message.to
            );
            if (!existingEdge) {
              return addEdge({
                id: `edge-${message.from}-${message.to}`,
                source: message.from,
                target: message.to,
                animated: true,
                label: 'message',
              }, prevEdges);
            }
            return prevEdges;
          });
        };

        socketService.on('message:received', handleMessageReceived);
        return () => socketService.off('message:received', handleMessageReceived);
      }, [isPaused]);

      return (
        <div className="communication-graph">
          <ReactFlow
            nodes={nodes}
            edges={edges}
            onNodesChange={onNodesChange}
            onEdgesChange={onEdgesChange}
            fitView
            className="graph-container"
          >
            <Background />
            <Controls />
            <MiniMap />
          </ReactFlow>
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/visualization/CommunicationGraph.tsx;
      grep -n "ReactFlow" frontend/src/components/visualization/CommunicationGraph.tsx;
      grep -n "addEdge" frontend/src/components/visualization/CommunicationGraph.tsx
    </automated>
  </verify>
  <done>CommunicationGraph component created with full message flow implementation</done>
</task>

</tasks>

<verification>
1. Build frontend and verify communication graph renders with React Flow
2. Send test messages and verify nodes/edges are created
3. Verify message flow is visualized with animated edges
4. Check that nodes are positioned correctly
</verification>

<success_criteria>
- [ ] Communication graph initializes with React Flow
- [ ] Nodes are created for message senders and receivers
- [ ] Edges are created for communication paths with animation
- [ ] Graph updates in real-time as messages are received
- [ ] Fit view functionality works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/10-real-time-visualization/10-03-SUMMARY.md`
</output>
