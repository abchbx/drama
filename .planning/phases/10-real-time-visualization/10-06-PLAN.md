---
phase: 10-real-time-visualization
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/routes/blackboard.ts"
]
autonomous: true
gap_closure: true
requirements: [UI-06]

must_haves:
  truths:
    - "User can view four-layer memory state and token budget usage"
  artifacts:
    - path: "src/routes/blackboard.ts"
      provides: "GET /api/blackboard/memory endpoint"
      contains: "blackboardRouter.get('/memory'"
  key_links:
    - from: "frontend/src/components/visualization/MemoryState.tsx"
      to: "src/routes/blackboard.ts"
      via: "fetch('/api/blackboard/memory')"
---

<objective>
Implement /api/blackboard/memory endpoint to provide complete memory state to frontend
Purpose: Create API endpoint that returns all four memory layers with token budgets for visualization
Output: GET /blackboard/memory route in src/routes/blackboard.ts
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/Coding/ClaudeCode/drama/src/routes/blackboard.ts
@d:/Coding/ClaudeCode/drama/src/services/blackboard.ts
@d:/Coding/ClaudeCode/drama/frontend/src/components/visualization/MemoryState.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /blackboard/memory endpoint</name>
  <files>src/routes/blackboard.ts</files>
  <action>
    Add a new GET endpoint to blackboardRouter that returns the complete memory state:

    1. Add route: `blackboardRouter.get('/memory', (req, res) => { ... })`

    2. The endpoint should:
       - Read the blackboard service from app.locals
       - Call readLayer for each of the four layers (core, scenario, semantic, procedural)
       - Build a response object with this structure:
         ```typescript
         {
           core: { tokensUsed: number, budget: number, entryCount: number },
           scenario: { tokensUsed: number, budget: number, entryCount: number },
           semantic: { tokensUsed: number, budget: number, entryCount: number },
           procedural: { tokensUsed: number, budget: number, entryCount: number }
         }
         ```
       - Return 200 with the memory state object

    3. Authentication: This endpoint should NOT require auth (for frontend visualization purposes). It's a read-only public endpoint for monitoring.

    4. Reference the existing readLayer method in BlackboardService:
       - Returns { layer, currentVersion, tokenCount, tokenBudget, entries }
       - tokenCount = tokensUsed, tokenBudget = budget
  </action>
  <verify>
    <parameter name="automated">grep -n "blackboardRouter.get.*memory" src/routes/blackboard.ts</parameter>
  </verify>
  <done>GET /api/blackboard/memory endpoint returns complete four-layer memory state with token budgets</done>
</task>

</tasks>

<verification>
1. Verify the /memory route is added to blackboardRouter
2. Verify endpoint reads all four layers
3. Verify response includes token usage and budgets for each layer
4. Verify endpoint does not require authentication (for frontend access)
5. Test endpoint returns valid JSON structure
</verification>

<success_criteria>
- [ ] GET /blackboard/memory route exists in blackboardRouter
- [ ] Endpoint returns all four memory layers (core, scenario, semantic, procedural)
- [ ] Each layer includes tokensUsed, budget, and entryCount
- [ ] Endpoint returns 200 with valid JSON
- [ ] Frontend MemoryState.tsx can successfully fetch memory state
</success_criteria>

<output>
After completion, create `.planning/phases/10-real-time-visualization/10-06-SUMMARY.md`
</output>
