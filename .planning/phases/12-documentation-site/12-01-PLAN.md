---
phase: 12-documentation-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs-site/package.json
  - docs-site/.vitepress/config.mts
  - docs-site/docs/index.md
  - docs-site/docs/guide/getting-started.md
autonomous: true
requirements:
  - DOC-01
  - DOC-05
must_haves:
  truths:
    - "VitePress site builds successfully"
    - "User can access documentation homepage at localhost:5173"
    - "Dark/light theme toggle works in browser"
    - "Mobile-responsive layout works"
  artifacts:
    - path: "docs-site/package.json"
      provides: "VitePress dependencies and scripts"
      min_lines: 15
    - path: "docs-site/.vitepress/config.mts"
      provides: "VitePress configuration with sidebar navigation"
      min_lines: 50
    - path: "docs-site/docs/index.md"
      provides: "Documentation homepage"
      min_lines: 30
    - path: "docs-site/docs/guide/getting-started.md"
      provides: "Getting started guide content"
      min_lines: 50
  key_links:
    - from: "docs-site/package.json"
      to: "VitePress build system"
      via: "npm scripts (docs:dev, docs:build)"
      pattern: "\"scripts\".*\"docs:dev\""
    - from: "docs-site/.vitepress/config.mts"
      to: "Markdown files in docs/"
      via: "sidebar configuration"
      pattern: "sidebar.*link:"
---

<objective>
Set up VitePress documentation site infrastructure with basic getting started guide and theme support.

**Purpose:** Establish the foundation for all documentation content. This creates the VitePress project structure, configures navigation and theming, and provides the minimal viable getting started guide.

**Output:**
- VitePress project initialized in `docs-site/` directory
- Development server running at http://localhost:5173
- Homepage and getting started guide accessible
- Dark/light theme toggle and mobile-responsive layout working
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-documentation-site/12-CONTEXT.md
@.planning/phases/12-documentation-site/12-RESEARCH.md
@/workspace/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize VitePress project</name>
  <files>docs-site/package.json</files>
  <action>Create VitePress project structure:

1. Create `docs-site/` directory at workspace root
2. Create `docs-site/package.json` with:
   ```json
   {
     "name": "drama-docs",
     "version": "1.0.0",
     "description": "Multi-Agent Drama System Documentation",
     "scripts": {
       "docs:dev": "vitepress dev",
       "docs:build": "vitepress build",
       "docs:preview": "vitepress preview"
     },
     "devDependencies": {
       "vitepress": "^1.0.0",
       "@vitepress/plugin-mermaid": "^2.0.0"
     }
   }
   ```
3. Install dependencies with `npm install`
4. Verify installation with `npx vitepress --version`</action>
  <verify>cd /workspace/docs-site && npm install && npx vitepress --version</verify>
  <done>VitePress installed successfully in docs-site/, package.json created with required dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Configure VitePress with navigation and theme</name>
  <files>docs-site/.vitepress/config.mts</files>
  <action>Create VitePress configuration file:

1. Create `docs-site/.vitepress/` directory
2. Create `docs-site/.vitepress/config.mts` with:
   ```typescript
   import { defineConfig } from 'vitepress'
   import mermaid from '@vitepress/plugin-mermaid'

   export default defineConfig({
     title: 'Multi-Agent Drama System',
     description: 'A shared-blackboard multi-agent drama orchestration system',

     themeConfig: {
       nav: [
         { text: 'Guide', link: '/guide/getting-started' },
         { text: 'API', link: '/api/index' },
         { text: 'User Guide', link: '/user-guide/sessions' },
         { text: 'Architecture', link: '/architecture/overview' },
         {
           text: 'GitHub',
           link: 'https://github.com/abchbx/drama'
         }
       ],

       sidebar: [
         {
           text: 'Guide',
           items: [
             { text: 'Getting Started', link: '/guide/getting-started' },
             { text: 'Quick Start', link: '/guide/quick-start' },
             { text: 'Concepts', link: '/guide/concepts' }
           ]
         },
         {
           text: 'API Reference',
           items: [
             { text: 'Overview', link: '/api/index' },
             { text: 'Authentication', link: '/api/authentication' },
             { text: 'Sessions', link: '/api/sessions' },
             { text: 'Blackboard', link: '/api/blackboard' },
             { text: 'Agents', link: '/api/agents' },
             { text: 'Endpoints', link: '/api/endpoints' }
           ]
         },
         {
           text: 'User Guide',
           items: [
             { text: 'Session Management', link: '/user-guide/sessions' },
             { text: 'Configuration', link: '/user-guide/configuration' },
             { text: 'Export', link: '/user-guide/export' },
             { text: 'Troubleshooting', link: '/user-guide/troubleshooting' }
           ]
         },
         {
           text: 'Architecture',
           items: [
             { text: 'Overview', link: '/architecture/overview' },
             { text: 'Components', link: '/architecture/components' },
             { text: 'Data Flow', link: '/architecture/data-flow' }
           ]
         }
       ],

       search: {
         provider: 'local',
         options: {
           locales: {
             zh: {
               translations: {
                 button: {
                   buttonText: '搜索文档',
                   buttonAriaLabel: '搜索文档'
                 },
                 modal: {
                   noResultsText: '无法找到相关结果',
                   resetButtonTitle: '清除查询条件',
                   footer: {
                     selectText: '选择',
                   }
                 }
               }
             }
           }
         }
       },

       socialLinks: [
         { icon: 'github', link: 'https://github.com/abchbx/drama' }
       ]
     },

     markdown: {
       config: (md) => {
         md.use(mermaid)
       }
     }
   })
   ```

This configuration per D-01 (independent site), D-02 (VitePress), D-06 (sidebar + breadcrumbs), D-07 (built-in search), D-10 (theme toggle). Sidebar structure follows D-03 four-tier navigation.</action>
  <verify>cd /workspace/docs-site && npx vitepress dev --port 5173 &> /dev/null & sleep 5 && curl -s http://localhost:5173 | grep -q "Multi-Agent Drama System" && pkill -f "vitepress dev"</verify>
  <done>VitePress config created with sidebar navigation, theme toggle, search, and Mermaid plugin configured</done>
</task>

<task type="auto">
  <name>Task 3: Create documentation homepage</name>
  <files>docs-site/docs/index.md</files>
  <action>Create homepage with project overview:

1. Create `docs-site/docs/` directory
2. Create `docs-site/docs/index.md` with content based on README.md sections:
   ```markdown
   ---
   layout: home
   hero:
     name: Multi-Agent Drama System
     text: A shared-blackboard multi-agent drama orchestration system
     tagline: Explicit cognitive boundaries, layered memory, real-time routing
     actions:
       - theme: brand
         text: Get Started
         link: /guide/getting-started
       - theme: alt
         text: View API Reference
         link: /api/index
   features:
     - icon: 🎭
       title: Shared Blackboard
       details: Four-layer memory model with token-budget-aware folding
     - icon: 🔒
       title: Cognitive Boundaries
       details: Programmatic capability enforcement for role-based isolation
     - icon: 🔄
       title: Real-time Routing
       details: Socket.IO-based broadcast, multicast, and peer messaging
     - icon: 🧠
       title: LLM Abstraction
       details: Unified interface for OpenAI, Anthropic, and Mock providers
   ---

   ## Status

   - **Milestone:** v1.2 (in progress)
   - **Test Status:** 104 tests passing
   - **Language:** TypeScript 5.5+
   - **Runtime:** Node.js 22 LTS

   ## What This Project Does

   The Multi-Agent Drama System coordinates a Director agent and multiple Actor agents to produce drama content with:

   - Shared state consistency through the blackboard pattern
   - Strict role-based memory boundaries
   - Character-scoped views for each agent
   - Long-context control through automated memory folding
   - Resilient routing with heartbeat and timeout handling

   ## Quick Links

   - [Getting Started Guide](/guide/getting-started) - Set up the system in minutes
   - [API Reference](/api/index) - Complete HTTP API documentation
   - [User Guide](/user-guide/sessions) - Session management and configuration
   - [Architecture](/architecture/overview) - System design and data flow

   ## GitHub

   Repository: [abchbx/drama](https://github.com/abchbx/drama)
   ```

Extract content from README.md, rewrite for documentation format per D-01 (independent site) and D-12 (getting started priority).</action>
  <verify>test -f /workspace/docs-site/docs/index.md && grep -q "Multi-Agent Drama System" /workspace/docs-site/docs/index.md</verify>
  <done>Homepage created with project overview, quick links, and hero section</done>
</task>

<task type="auto">
  <name>Task 4: Create getting started guide</name>
  <files>docs-site/docs/guide/getting-started.md</files>
  <action>Create getting started guide with step-by-step instructions:

1. Create `docs-site/docs/guide/` directory
2. Create `docs-site/docs/guide/getting-started.md` with:
   ```markdown
   # Getting Started

   This guide will help you set up and run the Multi-Agent Drama System on your local machine.

   ## Prerequisites

   - **Node.js 22 LTS** or later
   - **npm** or **yarn** package manager
   - **OpenAI API key** or **Anthropic API key** (for LLM providers)

   ## Installation

   ### 1. Clone the Repository

   ```bash
   git clone https://github.com/abchbx/drama.git
   cd drama
   ```

   ### 2. Install Dependencies

   ```bash
   npm install
   ```

   ### 3. Configure Environment Variables

   Copy the example environment file:

   ```bash
   cp .env.example .env
   ```

   Edit `.env` and add your API credentials:

   ```bash
   # LLM Provider Configuration
   LLM_PROVIDER=openai  # or "anthropic"
   OPENAI_API_KEY=your-openai-api-key
   OPENAI_MODEL=gpt-4o
   OPENAI_BASE_URL=https://api.openai.com/v1

   # Or for Anthropic:
   # ANTHROPIC_API_KEY=your-anthropic-api-key
   # ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

   # Server Configuration
   PORT=3000
   SOCKET_PORT=3001
   LOG_LEVEL=info

   # JWT Secret (minimum 32 characters)
   JWT_SECRET=your-super-secret-jwt-key-change-this

   # Blackboard Configuration
   BLACKBOARD_DATA_DIR=./data/blackboard
   CORE_LAYER_TOKEN_BUDGET=2000
   SCENARIO_LAYER_TOKEN_BUDGET=4000
   SEMANTIC_LAYER_TOKEN_BUDGET=8000
   PROCEDURAL_LAYER_TOKEN_BUDGET=1000
   ```

   ### 4. Start the Development Server

   ```bash
   npm run dev
   ```

   The server will start on:
   - **HTTP API:** http://localhost:3000
   - **Socket.IO:** http://localhost:3001

   ## Verification

   Check that the server is running:

   ```bash
   curl http://localhost:3000/health
   ```

   Expected response:

   ```json
   {
     "status": "ok",
     "timestamp": "2026-03-22T...",
     "snapshotLoaded": false,
     "services": {
       "blackboard": "connected",
       "router": "connected",
       "capability": "connected",
       "memory": "connected"
     },
     "config": {
       "llmProvider": "openai",
       "logLevel": "info"
     }
   }
   ```

   ## Next Steps

   - [Quick Start Guide](/guide/quick-start.md) - Create your first drama session
   - [Core Concepts](/guide/concepts.md) - Understand the blackboard architecture
   - [API Reference](/api/index.md) - Complete HTTP API documentation

   ## Troubleshooting

   ### Port Already in Use

   If you see "Port 3000 is already in use", change the `PORT` in `.env`:

   ```bash
   PORT=3001
   ```

   ### API Key Invalid

   Make sure your API key is valid and has sufficient quota:

   ```bash
   curl https://api.openai.com/v1/models -H "Authorization: Bearer $OPENAI_API_KEY"
   ```

   ### Build Errors

   Clear dependencies and reinstall:

   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```
   ```

This follows D-12 (content creation priority #1), extracts from README.md installation section, and provides clear step-by-step instructions per DOC-01.</action>
  <verify>test -f /workspace/docs-site/docs/guide/getting-started.md && grep -q "Getting Started" /workspace/docs-site/docs/guide/getting-started.md</verify>
  <done>Getting started guide created with installation, configuration, and troubleshooting steps</done>
</task>

</tasks>

<verification>
- VitePress dev server starts without errors (`npm run docs:dev`)
- Homepage loads at http://localhost:5173
- Dark/light theme toggle works in browser
- Mobile-responsive layout (sidebar collapses at mobile width)
- Getting started guide accessible and readable
</verification>

<success_criteria>
- Documentation site builds successfully
- User can access getting started guide
- Theme switching and responsive design work
- All navigation links configured
</success_criteria>

<output>
After completion, create `.planning/phases/12-documentation-site/12-01-SUMMARY.md`
</output>
