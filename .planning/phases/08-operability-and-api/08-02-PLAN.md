---
phase: 08-v1.2-operability-and-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/src/lib/api.ts, frontend/src/lib/socket.ts, frontend/src/store/appStore.ts, frontend/.env.example]
autonomous: true
requirements: [CFG-01, CFG-02]

must_haves:
  truths:
    - "Frontend app loads in browser without errors"
    - "API client reads base URL from environment variable"
    - "Socket client configured with reconnect settings from env"
  artifacts:
    - path: "frontend/package.json"
      provides: "Frontend dependencies (React, Zustand, socket.io-client)"
      contains: "react, zustand, socket.io-client"
    - path: "frontend/src/lib/api.ts"
      provides: "API client with base URL configuration"
      exports: ["apiClient", "ApiClient"]
    - path: "frontend/src/lib/socket.ts"
      provides: "Socket.IO client wrapper with reconnect logic"
      exports: ["socketService", "SocketService"]
    - path: "frontend/src/store/appStore.ts"
      provides: "Central state management for connection and sessions"
      exports: ["useAppStore"]
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "environment variables"
      via: "import.meta.env.VITE_API_BASE_URL"
      pattern: "import\\.meta\\.env\\.VITE_API"
    - from: "frontend/src/lib/socket.ts"
      to: "environment variables"
      via: "reconnect config from env"
      pattern: "VITE_SOCKET_.*"
---

<objective>
Bootstrap frontend React application with API and Socket.IO clients

Purpose: Establish the frontend foundation with environment-driven configuration for API base URL and Socket.IO reconnection settings. Create reusable client wrappers and central state store.

Output: Vite + React + TypeScript app, API client, Socket.IO service, Zustand store, environment configuration
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-v1.2-operability-and-api/08-CONTEXT.md
@.planning/phases/08-v1.2-operability-and-api/08-RESEARCH.md

# Backend API reference
@src/routes/sessions.ts
@src/types/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite React project</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/index.html</files>
  <action>
Create frontend directory with Vite + React + TypeScript scaffold.

1. Create frontend/ directory structure:
   - frontend/src/, frontend/src/lib/, frontend/src/store/, frontend/src/components/
   - frontend/public/

2. Create frontend/package.json with dependencies:
   - react, react-dom
   - typescript, @types/react, @types/react-dom
   - vite, @vitejs/plugin-react
   - zustand (state management)
   - socket.io-client
   - zod (form validation)

3. Create frontend/vite.config.ts with:
   - React plugin
   - Environment variable prefix: VITE_
   - Proxy configuration for API calls during dev (optional)

4. Create frontend/tsconfig.json with strict TypeScript settings

5. Create frontend/index.html as entry point
</action>
  <verify>
    <automated>cd frontend && npm install && npm run build</automated>
  </verify>
  <done>Frontend project created, dependencies installed, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create API client with env configuration</name>
  <files>frontend/src/lib/api.ts, frontend/.env.example</files>
  <action>
Build typed API client that reads base URL from environment.

1. Create frontend/.env.example with:
   - VITE_API_BASE_URL=http://localhost:3000
   - VITE_SOCKET_URL=http://localhost:3001
   - VITE_SOCKET_RECONNECTION_ATTEMPTS=5
   - VITE_SOCKET_RECONNECTION_DELAY_MS=1000
   - VITE_SOCKET_TIMEOUT_MS=5000

2. Create frontend/src/lib/api.ts with:
   - ApiClient class with configurable baseURL
   - Methods: getSessions(), getSession(id), createSession(input), startScene(id), stopScene(id)
   - Typed request/response using types from backend (mirror src/types/session.ts)
   - Error handling: extract error message from 4xx/5xx responses
   - Use import.meta.env.VITE_API_BASE_URL for baseURL
</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>API client created with typed methods, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create Socket.IO service with reconnect</name>
  <files>frontend/src/lib/socket.ts</files>
  <action>
Build Socket.IO client wrapper with automatic reconnection.

1. Create frontend/src/lib/socket.ts with:
   - SocketService class wrapping socket.io-client
   - Connection state: 'disconnected' | 'connecting' | 'connected' | 'reconnecting'
   - Configurable reconnect options from env:
     - reconnectionAttempts
     - reconnectionDelay
     - timeout
   - Events emitted: connection_state_change, session_state, scene_started, scene_stopped, scene_completed
   - Methods: connect(), disconnect(), on(event, callback), emit(event, data)

2. Implement reconnect logic:
   - Listen to socket.io built-in reconnect_attempt, reconnect, reconnect_error events
   - Update connection state and emit events for UI feedback
</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Socket service created with reconnect configuration, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 4: Create Zustand app store</name>
  <files>frontend/src/store/appStore.ts</files>
  <action>
Create central state management for connection state and sessions.

1. Create frontend/src/store/appStore.ts with:
   - Connection state: status ('disconnected' | 'connecting' | 'connected' | 'reconnecting'), lastError
   - Sessions state: sessions array, selectedSessionId, loading states
   - Actions:
     - setConnectionStatus(status, error?)
     - fetchSessions(), setSelectedSession(id)
     - createSession(input), updateSession(id, updates)
   - Use apiClient and socketService as singletons

2. Store should trigger API calls and socket subscriptions when actions called
</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Zustand store created with connection and sessions state, TypeScript compiles</done>
</task>

</tasks>

<verification>
- Frontend project builds without errors
- API client has typed methods matching backend endpoints
- Socket service has reconnect configuration from env vars
- Zustand store manages connection state and sessions
- Environment variables documented in .env.example
</verification>

<success_criteria>
- CFG-01 addressed: VITE_API_BASE_URL configurable
- CFG-02 addressed: Socket reconnect settings configurable
- Frontend foundation ready for UI development
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create .planning/phases/08-v1.2-operability-and-api/08-02-SUMMARY.md
</output>
