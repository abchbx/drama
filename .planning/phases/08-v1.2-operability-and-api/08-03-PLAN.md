---
phase: 08-v1.2-operability-and-api
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified: [frontend/src/components/SessionsList.tsx, frontend/src/components/CreateSessionForm.tsx, frontend/src/components/SessionPanel.tsx, frontend/src/App.tsx, frontend/src/index.css]
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "User can see list of created sessions"
    - "User can create new session via form"
    - "Created session appears in list immediately"
  artifacts:
    - path: "frontend/src/components/CreateSessionForm.tsx"
      provides: "Session creation form with validation"
      exports: ["CreateSessionForm"]
    - path: "frontend/src/components/SessionsList.tsx"
      provides: "Session list sidebar component"
      exports: ["SessionsList"]
    - path: "frontend/src/components/SessionPanel.tsx"
      provides: "Selected session details panel"
      exports: ["SessionPanel"]
  key_links:
    - from: "frontend/src/components/CreateSessionForm.tsx"
      to: "apiClient.createSession"
      via: "onSubmit handler"
      pattern: "onSubmit.*createSession"
    - from: "frontend/src/components/SessionsList.tsx"
      to: "useAppStore"
      via: "store subscription"
      pattern: "useAppStore\\(\\)"
    - from: "frontend/src/components/SessionPanel.tsx"
      to: "apiClient.getSession"
      via: "useEffect on selectedSessionId"
      pattern: "useEffect.*selectedSessionId.*getSession"
---

<objective>
Build session creation and list UI components

Purpose: Implement the user interface for creating drama sessions and viewing the session list. This addresses UI-01 by providing an interactive form and immediate feedback when sessions are created.

Output: React components for session list, create session form, and session details panel
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-v1.2-operability-and-api/08-CONTEXT.md
@.planning/phases/08-v1.2-operability-and-api/08-RESEARCH.md

# Frontend foundation from Plan 02
@frontend/src/lib/api.ts
@frontend/src/lib/socket.ts
@frontend/src/store/appStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionsList sidebar component</name>
  <files>frontend/src/components/SessionsList.tsx</files>
  <action>
Build session list sidebar showing all sessions.

1. Create frontend/src/components/SessionsList.tsx:
   - Left sidebar layout (250-300px fixed width)
   - Fetch sessions on mount via useAppStore
   - Map over sessions array to render session items
   - Each item shows: session name, status badge, created timestamp
   - Click to select session (sets selectedSessionId in store)
   - Highlight selected session
   - Empty state: "No sessions yet - create one!"

2. Status badge colors:
   - created/idle: gray
   - running: green (with pulse animation)
   - stopping: yellow
   - completed: blue
   - interrupted: orange
   - failed: red
</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>SessionsList component renders session list, selects on click, builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create CreateSessionForm component</name>
  <files>frontend/src/components/CreateSessionForm.tsx</files>
  <action>
Build session creation form with validation.

1. Create frontend/src/components/CreateSessionForm.tsx:
   - Form fields: Session name (text, required, 3-50 chars), Scene duration (number, 1-120 minutes), Agent count (number, 2-10)
   - Use zod for client-side validation
   - Submit button with loading state during creation
   - On success: clear form, toast notification, auto-select created session
   - On error: show error message below form

2. Form layout:
   - Positioned above session list or as modal
   - "Create Session" header
   - Input fields with labels
   - Submit button: "Create Session"
</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>CreateSessionForm validates input, creates sessions, handles errors, builds without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create SessionPanel detail component</name>
  <files>frontend/src/components/SessionPanel.tsx</files>
  <action>
Build main panel showing selected session details.

1. Create frontend/src/components/SessionPanel.tsx:
   - Display selected session details: name, status, duration, agent count
   - Show session metadata: created date, current scene ID (if running)
   - Empty state: "Select a session to view details" when no session selected
   - Status prominently displayed with icon/color

2. Layout:
   - Main panel (fills remaining space right of sidebar)
   - Header with session name
   - Status badge (large, prominent)
   - Details grid (2 columns on desktop, 1 on mobile)
</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>SessionPanel displays session details, handles empty state, builds without errors</done>
</task>

<task type="auto">
  <name>Task 4: Assemble app shell layout</name>
  <files>frontend/src/App.tsx, frontend/src/index.css</files>
  <action>
Wire components together in main App component.

1. Update frontend/src/App.tsx:
   - Two-panel layout: SessionsList (left) + SessionPanel (main)
   - CreateSessionForm positioned at top of sidebar or as floating button
   - Connection status indicator in corner (will be implemented in Plan 04)
   - Toast container for notifications

2. Add base styling in frontend/src/index.css:
   - Tailwind-style utility classes or styled-components
   - Flexbox layout for two-panel structure
   - Responsive breakpoints
</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>App shell assembled, all components wired, builds without errors</done>
</task>

</tasks>

<verification>
- Session list shows sessions from backend
- Create session form validates and submits
- New session appears in list after creation
- Session panel shows details of selected session
- Frontend builds without TypeScript or build errors
</verification>

<success_criteria>
- UI-01 addressed: User can create drama session via interactive web UI
- Two-panel layout working (sidebar + main panel)
- Form validation prevents invalid submissions
- Created sessions immediately visible in list
</success_criteria>

<output>
After completion, create .planning/phases/08-v1.2-operability-and-api/08-03-SUMMARY.md
</output>
